package Class15;

public class StaticVarDemo {
    // if an instance var has a keyword static is called a static variable
    // When we need to share something across the objects. we use static variable
    // it occupies the memory for long period of time
}
